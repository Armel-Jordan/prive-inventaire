# Build desktop (Tauri) : pas de même-origine, on cible l'API prod en absolu
# (comme l'app Android). À migrer vers https://<domaine>/api une fois le domaine en place.
VITE_API_URL=http://143.110.210.158/api
